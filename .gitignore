node_modules/
.next/
dist/

# packages/core/dist is intentionally tracked so GitHub consumers
# (`github:ochoadan/infernocms`) can `import from 'infernocms'` without
# having to build the package themselves.
!packages/core/dist/

.infernocms/
examples/*/uploads/
*.log
*.tsbuildinfo
.DS_Store

.env
.env.*
