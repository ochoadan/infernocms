// @infernocms/next — public entry. Most consumers will instead import from the
// generated `<project>/.infernocms-next/client.ts`. This entry exists for the
// runtime building blocks the generated client re-exports.

export const VERSION = '0.0.0';
